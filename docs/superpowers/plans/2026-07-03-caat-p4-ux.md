# CAAT Phase 4 — UX, Mobile, Accessibility, Dark Mode, Brand

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:executing-plans. Read the spec (Workstream D + Decisions) via `git show docs/caat-overhaul-spec:docs/superpowers/specs/2026-07-03-caat-overhaul-design.md`. Steps use `- [ ]`.

**Goal:** Make CAAT demo-grade and launch-grade on the surface: fix the mobile-breaking layouts, the fetch-failure-looks-empty pattern, the document self-verify hole, broken notification links, dark mode (Decision #6: fix for launch), accessibility gaps, and the brand-rule violations.

**Architecture:** UI-layer work on top of Phase 3. Verify by driving the app at desktop AND mobile viewport (375px), toggling dark mode, and simulating fetch failures. Group work by area; each task ends green.

**Tech Stack:** Next.js 16 App Router, React, Tailwind, Radix UI, dnd-kit.

## Global Constraints

- Work in `caat-frontend/`. **Binding brand rules** (this phase enforces them app-wide): no em dashes in any user-facing copy; no AI-looking emoji; no `rounded-full` on buttons/chips (moderate radii only — progress bars/avatars/status dots are fine). No Claude co-author/committer.
- **Stacked on Phase 3** (`perf/caat-p3-performance`). Base your branch there (Task 0).
- Post-P1/P3 structure: typed clients from `lib/supabase/*`; communities actions in `actions/*.ts` behind the barrel; scholarship logic in `lib/scholarship-filters.ts`; unified `components/BookmarkButton.tsx`; several pages now server-render first paint.
- Env: `cp /Users/macbook01/projects/Caat_V2/caat-frontend/.env.local caat-frontend/.env.local`; `npm install`. Restore Supabase if paused. Test account: test@gmail.com / testtest123.
- Verify at BOTH desktop and 375px mobile (browser devtools responsive mode), and in BOTH light and dark themes. Run `npm run typecheck && npm run build` after every task.
- Do NOT run git against the primary checkout `/Users/macbook01/projects/Caat_V2`; stay in your worktree; never `cd` out and `git add -A`.

---

### Task 0: Base the branch

- [ ] `git fetch origin && git checkout -b fix/caat-p4-ux origin/perf/caat-p3-performance`
- [ ] `git log --oneline -16` must show Phase 3 ("perf:"), Phase 1, Phase 2, Phase 0 commits. If not, STOP and report.
- [ ] `git show docs/caat-overhaul-spec:docs/superpowers/plans/2026-07-03-caat-p4-ux.md > docs/superpowers/plans/2026-07-03-caat-p4-ux.md && git add docs/superpowers/plans/2026-07-03-caat-p4-ux.md && git commit -m "docs: Phase 4 UX plan"`. Read the spec via git show.

---

### Task 1: Dashboard grid mobile reflow + touch/keyboard controls (D1, D11)

The grid uses a hardcoded `COLS=4` (`lib/grid.ts:10`) with percentage positioning and no breakpoints, and widget controls are hover-only (`opacity-0 group-hover`) and pointer-only. (Phase 2 already fixed the drag/resize pointer-capture bug.)

**Files:** `lib/grid.ts`, `components/dashboard/WidgetGrid.tsx`, `components/dashboard/WidgetCard.tsx`, `components/dashboard/widgets/CalendarWidget.tsx`, `TodoWidget.tsx`.

- [ ] **Step 1:** Make columns responsive: 1 column on phones (<640px), 2 on `sm/md`, 4 on `lg+`. Drive `COLS` off a breakpoint (CSS or a resize hook), and recompute widget positions/heights so nothing clips. Ensure CalendarWidget's calendar+events stack (not side-by-side overflow) below `sm`.
- [ ] **Step 2:** Make widget management usable without hover/pointer: show the remove/resize/priority controls on focus and on touch (not only `group-hover`); give the resize affordance a keyboard path (e.g. buttons to grow/shrink, or arrow-key handling on a focusable handle). Add `aria-label`s.
- [ ] **Step 3: Verify** at 375px, 768px, 1280px: widgets reflow and never clip/overflow; drag/resize still work (desktop); controls reachable by keyboard (Tab) and visible on touch. `npm run build` passes.
- [ ] **Step 4: Commit** — `fix(dashboard): responsive widget grid + touch/keyboard-accessible controls`.

---

### Task 2: Real error + loading + not-found states (D2, D3, D12)

Fetch failures currently render as "empty" everywhere; communities has no `loading.tsx`/`error.tsx`; there's no custom `not-found.tsx`.

**Files:** new `app/(main)/communities/loading.tsx`, `app/(main)/communities/error.tsx`, `app/not-found.tsx`; dashboard widgets (`ApplicationsRollup.tsx`, `UpcomingDeadlinesWidget.tsx`), `app/(main)/communities/CommunityFeedClient.tsx`, `schools-client.tsx`, the browse pages' error rendering.

- [ ] **Step 1:** Introduce a small shared `ErrorState` component (message + Retry button) and use it wherever a fetch can fail, distinct from the existing empty states. Dashboard widgets, community feed (`actions/feed.ts` returning `{posts:[]}` on error must instead signal error), schools bookmarked list, browse pages. A DB outage must look like an error with retry, not a brand-new empty account.
- [ ] **Step 2:** Add `app/(main)/communities/loading.tsx` (skeleton matching the feed) and `app/(main)/communities/error.tsx` (error boundary with reset). Wrap the tab-switch fetch in `CommunityFeedClient` in try/catch so a throw shows an error state, not a permanent skeleton.
- [ ] **Step 3:** Add a branded `app/not-found.tsx` with app chrome so `notFound()` on the three detail pages renders nicely.
- [ ] **Step 4: Verify** — temporarily force a fetch to throw (e.g. bad query) and confirm the error+retry state shows (not "empty"); navigate to a bogus `/scholarships/<uuid>` and see the branded 404; communities shows a skeleton then content, and a thrown tab-switch shows an error not a stuck skeleton.
- [ ] **Step 5: Commit** — `fix(ux): real error/retry, loading, and not-found states`.

---

### Task 3: Trust + engagement correctness (D4, D5, D7)

**Files:** `app/(main)/documents/client.tsx`, `app/(main)/communities/notifications/page.tsx`, `components/.../NotificationBell.tsx`, `app/(main)/communities/page.tsx` + `CommunityFeedClient.tsx`, `actions/feed.ts`.

- [ ] **D4:** Remove the user-facing document status control (`documents/client.tsx:171-250`) — students must not set their own docs to "Verified". Status becomes read-only/system-set. Verify the dropdown is gone and status still displays.
- [ ] **D5:** Fix notification links: follow notifications must link to `/communities/profile/${actor_id}` (id, not name) (`notifications/page.tsx:65`); the bell must not link to `/communities/${post_id}` when `post_id` is null (`NotificationBell.tsx:158`) — guard it. Verify both link types resolve, no 404 / `/communities/null`.
- [ ] **D7:** Like/save state must be correct for infinite-scroll posts: return per-viewer liked/saved state for posts as they load (extend `fetchPostsAction`/feed to include it, or fetch the sets for the newly loaded ids), not just the first 20. Verify a scrolled-in post shows correct like state and a single tap toggles correctly.
- [ ] **Commit** — `fix(communities,documents): remove self-verify, fix notification links, correct infinite-scroll like state`.

---

### Task 4: Mobile layout fixes for the heavy editors/tables (D8, D9, M4)

**Files:** `components/resume-builder/ResumeBuilderShell.tsx`, `components/majors/compare-table.tsx` + `majors/client.tsx`, `app/(main)/communities/page.tsx` + `c/[slug]/page.tsx`.

- [ ] **D8:** Resume builder: the fixed 360px + 520px panels overflow below ~884px. Make them responsive (stack or make the preview a toggle on small screens); make the top bar wrap instead of running off-screen at 375px. Verify no horizontal overflow at 375px/768px; the builder is usable on mobile.
- [ ] **D9:** Compare table (majors): make it a real `<table>` (or add proper `role`/scope for SR row/col association), give it a `min-width` and horizontal scroll container instead of crushing columns, remove the `overflow-hidden` clip. Verify at 375px it scrolls horizontally and is readable, and SR announces columns.
- [ ] **M4:** Communities sidebar is `hidden lg:block` with no mobile fallback, so a phone user can't join/manage a group. Provide a mobile entry point (a drawer or a top control) exposing My Communities/Browse and the group Join/Leave/Manage actions. Verify a phone user can join and manage a group.
- [ ] **Commit** — `fix(mobile): responsive resume builder, compare table, communities sidebar fallback`.

---

### Task 5: Dark mode — make the shipped toggle actually work (D10)

Decision #6: fix for launch. Many components hardcode light colors.

**Files (from the spec, plus any found):** `components/app-sidebar.tsx:113-186`, `app/(main)/applications/client.tsx:478`, `app/(main)/scholarships/client.tsx:504-521`, `components/.../FollowButton.tsx:40`, and any other hardcoded hex/`bg-white`/`text-black`/`border-black` found by audit.

- [ ] **Step 1: Audit** — `grep -rnE "bg-white|text-black|border-black|#[0-9a-fA-F]{6}|text-\[#|bg-\[#" app components | grep -v "dark:"` to find hardcoded light colors lacking a dark variant. Build the list.
- [ ] **Step 2: Fix** — replace hardcoded colors with theme tokens (the Tailwind/shadcn semantic classes: `bg-background`, `text-foreground`, `border-border`, `bg-muted`, etc.) or add `dark:` variants, so every surface is correct in dark mode. Prioritize the sidebar, applications fresh-import highlight, scholarships switcher, FollowButton, then the rest of the audit list.
- [ ] **Step 3: Verify** — toggle Dark on every major screen (dashboard, all browse pages + a detail each, applications, documents, essays, resume builder, communities, profile). No light-on-light or unreadable text; brand red links have a dark variant. Also check light mode didn't regress.
- [ ] **Step 4: Commit** — `fix(theme): make dark mode correct across the app`.

---

### Task 6: Accessibility batch (M6, M8, M11, M15, M16, L7)

**Files:** across profile cards, applications, documents, communities, pagination, dialogs.

- [ ] **M15 (labels/alt):** add `htmlFor`/`id` pairing or `aria-label` to unlabeled form controls (profile cards, applications date input, upload sheet, country/sort selects); `aria-label` on icon-only pagination chevrons and the TodoWidget delete; `alt` on avatars; fix the `aria-hidden` but focusable button in `NotificationBell.tsx:107`.
- [ ] **M8 (toggle state):** add `aria-pressed` to bookmark/like/save/filter toggles (schools/scholarships/majors, PostCard like/save, poll options, FeedTabs); give like/comment count-only buttons an accessible name.
- [ ] **M6 (contrast):** raise the amber status/deadline text (`text-amber-500/400`, ~2.2:1) to an AA-passing shade; add a non-color deadline-urgency cue (icon/text), not color alone; give brand-red links a dark-mode variant (overlaps Task 5).
- [ ] **M16 (dialogs):** convert the hand-rolled overlays (documents delete `documents/client.tsx:264-300`, community discard-post confirm `CreatePostForm.tsx:243-258`) to real Radix `Dialog`s (role, focus trap, Escape/backdrop close).
- [ ] **M11 (dnd a11y):** add a `KeyboardSensor` + `touch-action:none` to the resume DnD, or provide move-up/down buttons as a fallback.
- [ ] **L7 (semantics):** use `BreadcrumbPage` (not a hrefless `BreadcrumbLink`) for the current page; make card titles headings where appropriate.
- [ ] **Verify** — keyboard-only pass through a few key flows (Tab/Enter/Escape); a quick screen-reader spot check on toggles and the compare table; contrast check on the amber text. `npm run build` passes.
- [ ] **Commit** — `fix(a11y): labels, aria-pressed, contrast, real dialogs, keyboard dnd, semantics`.

---

### Task 7: Remaining UX polish + brand cleanup (M1, M3, M5, M7, M9, M10, M13, M14, L1, L2, L3, L4, L5, L6)

Group into a few commits.

- [ ] **Brand L1 (rounded-full):** replace `rounded-full` on interactive/badge elements across the ~14 files in the spec (`applications/client.tsx`, `schools-client.tsx`, `scholarships/[id]/page.tsx`, `scholarship-card.tsx`, `major-card.tsx`, `compare-table.tsx`, profile cards, `ApplicationsRollup.tsx`) with a moderate radius (e.g. `rounded-md`/`rounded-lg`). Leave progress bars, avatars, status dots.
- [ ] **Brand L2 (em dashes):** remove em dashes from user-facing copy/toasts (`OnboardingChecklist.tsx:54`, `applications/client.tsx:146`, `SectionEditorPanel.tsx:115`, `my-scholarships-panel.tsx:56,281,342`, `lib/profile-utils.ts:30`, `actions/*.ts` toasts) — use commas/colons/separate sentences.
- [ ] **Brand L3 (glyphs):** replace decorative `★`/`✓`/`←→` text glyphs used as UI with proper icons or remove.
- [ ] **M13 (off-brand color):** replace the blue accents on scholarship detail (`scholarships/[id]/page.tsx:411,161`) and the school country badge with the brand palette; unify the chip style (avoid two design languages).
- [ ] **M1 (confirm/data-loss):** add confirmation to resume-section delete and leave-group; add a `beforeunload` guard where essays/resume have a 2s autosave window (complements the Phase 2 flush fixes). Convert the documents delete overlay to the real dialog (done in Task 6).
- [ ] **M9:** add a deadline setter on the application hub (`updateApplicationDeadline` exists, unused) so "No deadline set" is actionable.
- [ ] **M10:** restore View/Delete on "resubmit" document rows (`documents/client.tsx:740-748`).
- [ ] **M14:** return `has_requested` from `fetchGroupsAction` and reflect it (don't always show "Request to Join").
- [ ] **M7:** run `enrichPosts` on permalink + profile posts so polls/saves/verified badge render there.
- [ ] **M3:** stop marking notifications read during server render (`notifications/page.tsx:27`); mark read on an explicit view action.
- [ ] **M5 / L5 / L6:** align `loading.tsx` skeletons to real layouts (reduce shift); add CTAs to the empty bookmarked-schools/majors states; remove dead `ApplicationReadiness.tsx` and the double "Applications" heading.
- [ ] **L4:** make onboarding dismissal server-persisted (a profile/settings flag) instead of localStorage-only.
- [ ] **Commit** each sub-group with a descriptive `fix(...)`/`style(...)` message.

---

## Phase verification gate

- [ ] `npm run typecheck` PASS, `npm run lint` clean (no new warnings), `npm run test:unit` PASS, `npm test` PASS, `npm run build` PASS.
- [ ] At 375px: dashboard, resume builder, compare table, communities all usable with no horizontal overflow.
- [ ] Dark mode correct on every major screen; light mode unregressed.
- [ ] Fetch failures show error+retry (not empty); branded 404; notification links resolve.
- [ ] Documents can't be self-verified; a11y spot-check (keyboard + aria-pressed + contrast) passes.
- [ ] No `rounded-full` on buttons/chips, no em dashes in touched copy, no decorative glyph-as-UI.

## PR

**Title:** `fix: Phase 4 — UX, mobile, dark mode, accessibility, brand`

**Body:**
```
Phase 4 of the CAAT overhaul (spec Workstream D). Surface quality for launch/demo:
- Responsive dashboard grid + touch/keyboard-accessible widget controls
- Real error/retry, loading, and not-found states (fetch failure no longer looks "empty")
- Removed document self-verify; fixed notification links; correct infinite-scroll like state
- Mobile fixes: resume builder, compare table, communities sidebar fallback
- Dark mode fixed across the app (Decision #6)
- Accessibility: labels/alt, aria-pressed, contrast, real dialogs, keyboard dnd
- Brand cleanup: no rounded-full pills, no em dashes, no decorative glyphs; unified chip colors
- Plus the medium/low UX batch (deadline setter, resubmit actions, has_requested, enrichment on permalinks, onboarding persistence, dead-code removal)

Stacked on #144/#143/#142/#141 — merge P0 -> P2 -> P1 -> P3 -> P4.
```
